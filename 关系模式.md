# 图书管理系统 — 关系模式

> 基于 [`init_database.py`](init_database.py) 中的 `SCHEMA_SQL` 定义。

---

## 1. BOOK（图书表）

| 属性 | 类型 | 约束 | 说明 |
|------|------|------|------|
| **bookid** | INT | `PRIMARY KEY` | 图书编号 |
| **bookname** | TEXT | `UNIQUE NOT NULL` | 书名（不可重复） |
| **status** | TEXT | `CHECK(status IN ('borrowed', 'returned')) DEFAULT 'returned'` | 借阅状态 |

**关系模式**：

```
BOOK (bookid, bookname, status)
```

> 主键：<u>bookid</u>  
> 候选键：bookname

---

## 2. USER（用户表）

| 属性 | 类型 | 约束 | 说明 |
|------|------|------|------|
| **userid** | INT | `PRIMARY KEY` | 用户编号 |
| **username** | TEXT | `UNIQUE NOT NULL` | 用户名（不可重复） |
| **currentborrow** | INT | `DEFAULT 0 CHECK(currentborrow >= 0 AND currentborrow <= 5)` | 当前借书数量（上限5本） |

**关系模式**：

```
USER (userid, username, currentborrow)
```

> 主键：<u>userid</u>  
> 候选键：username

---

## 3. BORROW（借阅记录表）

| 属性 | 类型 | 约束 | 说明 |
|------|------|------|------|
| **borrowid** | INT | `PRIMARY KEY` | 借阅记录编号 |
| **userid** | INT | `NOT NULL FOREIGN KEY REFERENCES USER(userid)` | 借阅用户编号 |
| **bookid** | INT | `NOT NULL FOREIGN KEY REFERENCES BOOK(bookid)` | 借阅图书编号 |

**关系模式**：

```
BORROW (borrowid, userid, bookid)
```

> 主键：<u>borrowid</u>  
> 外键：userid 参照 USER(userid)，bookid 参照 BOOK(bookid)

---

## E-R 图

```
┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│     USER     │         │    BORROW    │         │     BOOK     │
├──────────────┤         ├──────────────┤         ├──────────────┤
│ userid   PK  │───1:N───│ borrowid PK  │───N:1───│ bookid   PK  │
│ username UQ  │         │ userid   FK  │         │ bookname UQ  │
│ currentborrow│         │ bookid   FK  │         │ status       │
└──────────────┘         └──────────────┘         └──────────────┘
```

> USER 与 BORROW 是 **1:N** 关系（一个用户可有多条借阅记录）  
> BOOK 与 BORROW 是 **1:N** 关系（一本书可被多次借阅，但同一时刻最多一条活跃记录）

---

## 触发器

| 触发器 | 时机 | 功能 |
|--------|------|------|
| `trg_borrow_insert` | `AFTER INSERT ON BORROW` | 阻止已借出书籍重复借阅（RAISE ABORT）；自动更新 `BOOK.status='borrowed'` 和 `USER.currentborrow+1` |
| `trg_borrow_delete` | `AFTER DELETE ON BORROW` | 还书时自动恢复 `BOOK.status='returned'` 和 `USER.currentborrow-1` |

---

## 视图

| 视图 | 说明 |
|------|------|
| **V_USER_BORROW** | 用户借阅明细（JOIN USER、BORROW、BOOK 三表） |
| **V_BORROW_RANK** | 用户借阅排行（按 currentborrow 降序） |
